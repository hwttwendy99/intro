# Popover 气泡弹框

与 Tooltip 结构相同，区别：
- Padding: **8px**（比 Tooltip 的 6px 12px 更均匀）
- Line-height: **22px**（比 Tooltip 的 20px 大）
- Min-width: 120px

## CSS 来源

完整样式见 [`_css/popover.css`](_css/popover.css)，生成 HTML 时直接复制该文件内容到 `<style>` 中。
