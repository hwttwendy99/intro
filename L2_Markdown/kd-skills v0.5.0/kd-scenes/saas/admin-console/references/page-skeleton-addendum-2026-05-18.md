# Page Skeleton Addendum (2026-05-18)

本补充文件记录 2026-05-18 在管理后台表格页复盘中确认的页面骨架级硬规则，用于补强 `page-skeleton.md`。

## 描述栏

- 描述栏高度固定为 `32px`。
- 描述栏文本默认在行高内做底对齐；HTML 预览可用 `align-items: flex-end` 或等价 Auto Layout 实现，不使用垂直居中模拟。
- 描述栏只承载单行 `13px / 20px` 的说明文案，不得混入按钮、标签、状态元素或重复页面标题。

## 使用方式

- 当任务涉及管理后台表格页骨架修复、HTML 预览校对或“为什么描述栏视觉不对”的归因时，一并读取本文件。
- 若后续主规则文件编码整理完成，应把本文件内容并回 `page-skeleton.md`，避免长期双维护。
